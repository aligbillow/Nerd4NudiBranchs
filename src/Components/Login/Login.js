import React from "react";

const Login = () => {
    return (
        <div style={{display: "flex", justifyContent: 'flex-end'}}>
        <p>Login</p>
        </div>
    );
} 

export default Login;
